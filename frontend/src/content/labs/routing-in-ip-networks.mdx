---
title: Маршрутизация в IP сетях
publishedAt: 2024-03-22
sortOrder: 1
categories:
  - IP
  - Routing
---

# Обзор

## Цель работы

Изучить принципы маршрутизации, научиться настраивать статические маршруты и проверять связность сети в среде GNS3.

<Separator orientation="horizontal" className="mt-12" />

## Краткие теоретические сведения

Маршрутизаторы (аппаратные или программные) выполняют задачу выбора оптимального маршрута следования IP пакета и его отправки по этому маршруту. Для принятия решения анализируется адрес получателя и устанавливается маршрут следования на основе неких формализованных записей о структуре составной сети. Эти записи называются таблицами маршрутизации

В таблице маршрутизации присутствуют как минимум следующие поля: адрес назначения (адрес IP-сети или IP адрес хоста), идентификатор порта, через который пакет идет до сети назначения (порт обозначается IP-адресом или внутренним номером), шлюз (IP адрес на который необходимо пойти после того как пакет покинет порт), метрика (показатель качества маршрута).

На каждом маршрутизаторе сети присутствует таблица, полностью описывающая структуру всей сети и иногда содержащая записи о маршрутах по умолчанию.

Таблицы маршрутизации составляются вручную или с помощью протоколов внутренней маршрутизации, автоматизирующих этот процесс. Одним из таких протоколов является протокол RIP2.

Эти протоколы используются внутри автономных систем. Автономная система (AS) в интернете — это система IP-сетей и маршрутизаторов, управляемых одним или несколькими операторами, имеющими единую политику маршрутизации с Интернетом

Автономные системы обмениваются маршрутной информацией через протоколы внешней маршрутизации, например BGP. BGP (англ. Border Gateway Protocol, протокол граничного шлюза) — основной протокол динамической маршрутизации в Интернете. Он предназначен для передачи обобщённых маршрутов между различными сетями (автономными системами) – т. е. фактически распространяет информацию о том, какие сети принадлежат какой AS и как AS подключены друг к другу. В частности, стык между двумя провайдерами организуется через BGP.

<Separator orientation="horizontal" className="mt-12" />

## Порядок выполнения работы

В данном разделе описаны шаги, которые необходимо выполнить для успешного завершения работы. При возникновении затруднений обращайтесь к чат-боту.

Делайте скриншоты и сохраняйте конфигурации оборудования, чтобы в дальнейшем включить их в отчет.

<Subtitle>
  ## Часть 1. Настройка инфраструктуры
</Subtitle>

<Slot className="mt-6">### Моделирование схемы</Slot>

1. Реализуйте схему, приведенную на рисунке ниже, смоделировав ее в программе `<название программы>`.
   - Расположите по одному компьютеру в сети №1, №4, №5
   - Дайте название компьютерам PC1, PC4 и PC5 соответственно.

<Callout>
  Обратите внимание, что сеть №3 — это одна локальная сеть, в которой
  маршрутизаторы соединяются через коммутатор.
</Callout>

<Image
  src="/labs/routing-in-ip-networks/scheme.png"
  width={800}
  height={350}
  alt="Схема сети"
  className="mt-4 rounded-lg border"
/>

2. Используйте следующее сетевое оборудование:
   - коммутатор Ethernet Switch (1 шт),
   - маршрутизатор Cisco 3745 (4 шт),
   - компьютеры PC (3 шт),
   - коммуникационные модули и кабели.

### Настройка IP-адресов

<Tabs defaultValue="pc1">
  <TabsList>
    <TabsTrigger value="pc1">PC1</TabsTrigger>
    <TabsTrigger value="pc4">PC4</TabsTrigger>
    <TabsTrigger value="pc5">PC5</TabsTrigger>
  </TabsList>

<TabsContent value="pc1">
  ```bash ip 192.168.1.0/24 gateway 192.168.1.10 ```
</TabsContent>

<TabsContent value="pc4">
  ```bash ip 192.168.4.0/24 gateway 192.168.4.10 ```
</TabsContent>

  <TabsContent value="pc5">
    ```bash
    ip 192.168.5.0/24
    gateway 192.168.5.10
    ```
  </TabsContent>
</Tabs>

### Проверка связи

Проверьте доступность ближайших соседей в локальных сетях

```bash
# На PC1
ping 192.168.1.1   # Шлюз LAN1
ping 192.168.4.10  # PC4 (должен FAIL)
```

<Separator orientation="horizontal" className="my-12" />

<Subtitle className="mt-0">
  ## Часть 2. Настройка статической маршрутизации
</Subtitle>

<Slot className="mt-6">### Добавление маршрутов</Slot>

Настройте на маршрутизаторах (пример для R1):

```cisco
ip route 192.168.4.0 255.255.255.0 10.0.0.2
ip route 192.168.5.0 255.255.255.0 10.0.1.2
```

### Проверка связности

Проверьте возможность передачи пакетов данных между всеми узлами модели

```bash
# На PC1
ping 192.168.4.10  # Успешный ответ
ping 192.168.5.10  # Успешный ответ
```

### Сохранение таблиц маршрутизации

Выведите на консоль и сохраните таблицы маршрутизации всех маршрутизаторов

```bash
PC4> show
# Пример вывода:
# NAME   IP/MASK              GATEWAY           MAC                LPORT  RHOST:PORT
# PC4    192.168.4.10/24      192.168.4.1       00:50:79:66:68:01  10032  127.0.0.1:10033
#        fe80::250:79ff:fe66:6801/64
```

<Separator orientation="horizontal" className="mt-8" />

## Артефакты для отчета

1. Скриншоты таблиц маршрутизации с каждого маршрутизатора.
2. Результаты команд `ping` между всеми узлами.
3. Конфигурации интерфейсов (используйте `show running-config`).

<Callout variant="success" className="mt-6">
  **Готово!** Вы настроили статическую маршрутизацию между подсетями.
</Callout>
